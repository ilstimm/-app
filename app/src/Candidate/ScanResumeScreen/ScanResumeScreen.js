import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ScanResumeScreen = ({navigation}) => {
  return (
    <View>
      <Text>ScanResumeScreen</Text>
    </View>
  );
};

export default ScanResumeScreen;

const styles = StyleSheet.create({});
